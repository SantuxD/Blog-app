<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include("partials/head") %>

    <title></title>
</head>

<body>
  <%- include("partials/navbar") %>
    <%- include("partials/script") %>
      <div class="container mt-5">
        <h1>
          <%= blog.title %>
        </h1>
        <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="..." />
        <p>
          <%=blog.body%>
        </p>
      </div>
      <div class="container mt-5">
        <% if (blog.createdBy) { %>
          <img src="<%= blog.createdBy.profileImageURL || '/images/default.png' %>" alt="Author">
          <h3>Author: <%= blog.createdBy.fullName %>
          </h3>
          <% } else { %>
            <h3>Author: Unknown</h3>
                <% } %>
       </div>
    </div>
      <div class="container mt-5">
        <h2>Comments(<%=comments.length %>) </h2>
        <% if (locals.user) { %>
         
      
        <form action="/blog/comment/<%=blog._id %>" method="post">
          <div class="mb-3">
            <input type="text" class="form-control" name="content" placeholder="Enter your  comment" required />
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
          <% } %>
      </div>
      <div class="mt-3">
        <% comments.forEach(comment =>{ %>

          <div>
            <img src="<%= comment.createdBy.profileImageURL %>" width="50px"/>
        <%= comment.createdBy.fullName  %>
        <pre><%= comment.content %></pre>
            </div>
         
       <% }) %>
        
            

      </div>
</body>
</html>